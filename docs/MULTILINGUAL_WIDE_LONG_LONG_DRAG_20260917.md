# 와이드→롱 다중 선택·자동 스크롤 검사 — 2026-09-17

`validate_wide_long_long_drag_browser.cjs`로 격리된 전체 앱에서 합성 자료 20열을 반복 열 목록으로 옮겼다. Ctrl 선택으로 처음 두 항목을 함께 선택한 뒤 실제 마우스 포인터를 목록 하단에 유지해 자동 스크롤을 발생시키고 두 항목을 맨 뒤로 이동했다.

8언어 각각에서 목록이 실제 하단까지 스크롤되었는지, 순서가 `x3…x20,x1,x2`인지, 두 항목의 선택 표시가 유지되는지 확인했다. 해당 순서로 변환 그룹을 만들어 2개 원본 행 × 20시점 = 40개 변환 행의 ID·시점·값을 모두 대조했다. 검사 후 그룹을 삭제하고 다음 언어에서 같은 절차를 반복한다.

로그: `tmp/wide-long-long-drag-browser.log`.

8언어 모두 통과했고 pageerror는 없었다.

이번에는 검사와 기록만 추가했다. 제품 코드 변경·분석 출력/내보내기 변경·설치본 생성은 없다. 모든 목록 길이·모든 스크롤 방향·모든 다중 선택 조합을 망라한 검사는 아니며, 네이티브 저장 창의 실제 동작은 별도 미완료다.
