# GLM MI 오류 안내 점검

2026-09-16. 설치본은 만들지 않았다.

- MI 통합 시 표준오차 방법, 분포/링크, 사례 수/잔차 자유도, 계수 항 불일치 및 잔차 자유도 누락의 고정 안내 5개를 번역했다.
- 전체 계수표 요구, 불일치 대치 번호, 모형 적합 실패의 동적 안내 3개를 번역했다. 개수, 대치 번호 및 외부 상세 오류(줄바꿈·사용자명·퍼센트 포함)를 그대로 보존한다.
- `generalized_error_ui_text`의 표시 변환만 확장했다. 분석 엔진과 출력 표·저장 경로는 변경하지 않았다.
- `scripts/validate_glm_mi_errors_i18n.R`: 8개 언어 통과. 실제 통합 함수의 누락 계수표/항 불일치/표준오차 불일치/자유도 누락 오류 4개를 재현했다. 다른 분기는 메시지 fixture로 검사했다.
- 기존 입력 오류 8개 언어 검사 및 i18n 계약 검사 통과.
- 실패 알림만 변경했으므로 결과 파일 재검증 대상은 없다. 데스크톱 알림 시각 검증은 수행하지 않았다.
- Rubin 스칼라 통합 계산 오류, 대치 완료 실패 등 미등록 안내는 추가 점검 대상이다. 전체 다국어 완료를 의미하지 않는다.
