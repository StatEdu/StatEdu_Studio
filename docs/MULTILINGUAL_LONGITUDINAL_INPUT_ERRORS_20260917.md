# 종단·패널 입력 실패 안내 — 2026-09-17

실행 실패 알림에서 영어 원문으로 출력되던 상세 7종을 번역 테이블에 연결했다: 이분형 범주/값 수 2종, 양의 유한 가중치, 가중치 행 대응, 가중치 변수 선택, 가중치 결측/양수 조건, Gaussian GLMM 선택 제한. 정확히 일치하는 앱 오류만 번역하고 미등록 외부 상세·사용자 문자열은 유지한다.

`validate_longitudinal_input_errors_i18n.R`에서 실제 함수로 7종 오류를 발생시킨 후 8개 언어와 한국어 복귀를 검사했다. 실제 서버 실패 콜백을 추출해 결과 초기화·번역 접두어와 상세·오류 유형·8초 표시도 확인했다. 정상 이분형 변환과 가중치 처리도 확인했다. 테스트는 통과했다.

분석 결과 표·계산·저장은 변경하지 않았다. 실제 브라우저 알림 검사는 포함하지 않는다. GEE 수렴·상관행렬, 반복 LMM 고유 오류 및 동적 상세 문구는 아직 이 변경의 범위 밖이다. 메타분석은 사용자의 요청대로 추가 작업 대상에서 제외했다. 설치본은 만들지 않았다.
