# 종단 설정 옵션·독립변수 블록 — 2026-09-17

실험적 SPSS 호환 GEE와 LMM의 ML 확률효과·REML UN·REML AR(1) 옵션 4개, 독립변수 개수 블록 제목을 8언어로 연결했다. 기존 한국어 외 언어에서 영어 원문이 남던 표시를 보완했다. 옵션 내부 값과 분석 계산은 변경하지 않았다.

`validate_longitudinal_option_labels_i18n.R`에서 모형·분포·상관구조·가중치·결측 처리·대체 설정 등 16개 옵션 묶음의 8언어 표시와 내부 값 보존을 검사했다. AR(1)은 통용 표기를 허용한다.

GEE 실험 모드, LMM ML/UN/AR(1), GLMM, 패널 FE/RE의 7개 설정 × 8언어로 생성한 HTML 56개를 확인했다. 독립변수 제목 번역, 상태 객체의 변수 배치·설정, 실제 select의 선택된 값 하나가 원래 값인지 확인했다. 이는 생성 상태·HTML 검사이며 실제 브라우저의 언어 변경 이벤트와 사용자 클릭 검증은 아니다.

모든 검사가 통과했고 변경 파일의 `git diff --check`도 통과했다. 이번 변경은 설정 화면에만 적용되므로 결과표·5종 저장 검사는 재실행하지 않았다. 다른 설명·상태 유지 조합과 실제 브라우저 왕복 검증은 남아 있다. 메타분석과 설치본은 변경하지 않았다.
