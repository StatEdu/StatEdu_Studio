# KM 화면 수정 설치본 반영 및 실행 검증

2026-09-15, StatEdu Studio 1.3.0 Windows x64.

## 반영 내용

- KM 위험표 행 간격, 실제 글자 폭 기준 주석 줄바꿈, 집단 수에 따른 그림 높이 조정을 설치본에 반영했다.
- HWPX 변환 후 소유한 한글 프로세스가 이미 종료된 경우의 정리 경합 보완을 반영했다.
- 이번 단계에서는 제품 계산 코드를 추가 변경하지 않았다. 이전 화면/내보내기 검증은 `PERFORMANCE_KM_SCREEN_LAYOUT_20260915.md` 참조.

## 설치 파일

- 경로: `dist/electron/StatEdu_Studio_Setup_1.3.0.exe`
- 크기: 324,297,588 bytes
- SHA256: `3C317E89A4BA78DC0AA27EC2A5D0D85DF1C0B2E9AC0439044B3DD22AEA183514`
- 번들 R 4.5.3 및 기존 라이브러리를 재사용했다. SmartPLS 증거 검사를 포함한 필수 빌드 검사를 생략하지 않았다.

## 검증 결과

- 설치 회귀 검사 전체 통과. 매개·조절 10,000회 독립 실행 3회에서 결과/난수 상태 정확 일치, 작업 시간 3.050/2.970/3.010초.
- 구조모형 성능 검사 통과: CFA 1,000회 9.649초, SEM 5,000회 132.593초, PLS-SEM 1,000회 1.698초. 기존 실행과 통제된 A/B 성능 비교는 아니다.
- 번들 준비 및 최종 패키지에서 PLS/PLSc 검사 각 19개 통과. 패키지 전체 R 모듈 로딩 통과.
- 필수 `verify_packaged_result_updates.ps1` 실행: R/web/HWPX helper 337개 파일의 현재 소스 SHA256과 패키지 SHA256 일치.
- 최종 패키지 실행 파일을 격리 프로필에서 실행해 72행 자료의 상관/KM/Cox 분석을 확인했다. 표 4/12/14개, 그림 2/4/3개 로딩, JavaScript pageerror 0개.
- 실제 KM 화면 PNG에서 위험표 두 행의 간격과 주석 두 줄 전체 표시를 확인했다. 캡처 상단에는 고정 탐색 메뉴가 겹쳐 있으므로 그림 전체의 픽셀 비교 증거로 사용하지 않는다.
- 누적 결과 3개 저장 후 재실행 자동 복원에서 ID/순서/HTML 정확 일치. 첫 준비 3.634초, 복원 재실행 3.524초로 각각 1회 관측이다.
- 두 실행 모두 창 닫기 후 Electron 및 R 종료 로그 확인.

## 증거와 범위

빌드/회귀/패키지 소스 검증 기록은 `output/km-layout-installer-20260915/`, GUI 이미지와 저장/복원 기록은 `tmp/packaged-smoke-lGipcS/`에 있다. 현재·누적 HTML/PDF/Word/HWPX/Excel 내용 검증은 이전 KM 화면 수정 단계에서 수행했으며, 이번 패키지 단계의 GUI 검사는 세 분석의 화면·누적 HTML·복원을 대상으로 한다.

기존 사용자 설치에 대한 업그레이드, 사용자 프로필 이관, 모든 분석 화면의 전체 QA 및 외부 배포는 수행하지 않았다. 다음 검토에서는 이 설치본을 기준으로 남은 성능 후보를 선정한다.
