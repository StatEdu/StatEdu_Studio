# 정규·이항·감마 자동 선택 결과 — 2026-09-17

자동 분포 선택의 정규 GEE, 이항 GEE/GLMM, 감마 GEE/GLMM을 실제 합성 자료로 실행했다. 적합 세부정보의 `Singular fit`, `Random-effect variance` 두 항목에 한국어 외 번역 누락이 있어 8언어 사전 키로 연결했다. 계산·자동 선택 규칙은 변경하지 않았다.

`scripts/validate_longitudinal_auto_families_i18n.R`의 5경로 × 8언어 40개 검사가 통과했다. 선택 분포와 계수 표 반환, 실제 결과 패널의 본 표가 영어 기준과 동일한지, 보고 점검표·적합 세부정보의 번역, 숫자 문자열과 원본 결과 보존을 확인했다. 스페인어 `No`는 영어와 철자가 같은 정상 번역이므로 사전 값으로 검증한다.

이항 결과변수는 이분형 측정수준을 지정했다. 측정수준 없는 이항 자료나 모든 변수 라벨·분포·고급 옵션 조합의 검증을 뜻하지 않는다. 합성 모형에서 특이 적합 및 수렴 경고가 발생했으며, 번역 검증 통과는 모형 수렴 보장을 의미하지 않는다. 외부 패키지 경고 전문의 번역도 이번 범위가 아니다.

저장 검증은 실제 적합 세부정보를 `tmp/longitudinal-auto-families/details.rds`에 보존한 뒤 `scripts/validate_longitudinal_count_exports.R`의 입력 인자로 재사용한다. 저장 시 분석을 다시 실행하지 않는다. 출력 경로는 `tmp/longitudinal-auto-family-exports`다. 실제 전체 패널 본 표는 위 40개 검사로 확인하며, 저장 묶음에는 영향받는 보조표와 별도의 영어 계수 시험 표를 넣는다.

실제 브라우저 언어 왕복·페이지별 시각 검토와 다른 모형/옵션 조합은 남아 있다. 메타분석과 설치본은 변경하지 않았다.

한국어·일본어 현재/누적 HTML·PDF·Word·직접 생성 HWPX·Excel 저장 검증이 모두 통과했다. HTML·문서 XML·Excel 셀에서 캡처 문구를 확인했고, PDF 4개(각 2쪽)도 모든 캡처 문구를 포함했다. 변경 파일의 `git diff --check`도 통과했다.
