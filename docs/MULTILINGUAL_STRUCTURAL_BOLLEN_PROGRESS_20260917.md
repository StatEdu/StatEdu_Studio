# Bollen–Stine 진행/적용 조건 안내 — 2026-09-17

- 동기 실행 경로의 진행 제목, 요청 반복 수, 완료/전체/유효 반복 수, 결과 준비 메시지를 UI 언어에 연결했다. 실행 함수에서 현재 언어를 명시적으로 전달한다.
- 실행 전 적용 조건 검사에서 ML 전용, 순서형 불가, 단일집단 전용, 포화모형, 완전 자료 필요 안내를 번역한다. 엔진의 원본 조건 판정과 사유 문자열은 유지하고 UI에 전달할 때 번역한다.
- `scripts/validate_structural_bollen_progress_i18n.R`: 실제 CFA에 같은 시드로 언어별 4회 부트스트랩을 실행하여 8개 언어의 결과 데이터가 완전히 동일함을 확인했다. 반복 수는 표시/동작 검증용이며 통계적 정밀도 검증용이 아니다.
- 진행 API를 테스트에서 기록해 숫자 치환과 명시적 언어 전달을 확인했다. 실제 창에서의 시각 검증은 이번 범위에 포함하지 않았다.
- 5종 적용 조건 문구 번역 검사 통과. 포화모형 및 비ML 추정의 실제 조건 판정도 확인했다. 나머지 조건은 사전 검증 범위다. 비CFA 실행 생략 동작 유지 확인.
- 다국어 공통 검사와 diff 공백 검사 통과. 로그: `tmp/structural-bollen-progress-{validation,coverage}.log`.
- 분석 결과 표/수치/내보내기는 변경하지 않았으므로 이번에는 5형식 저장 검사를 재실행하지 않았다.

비동기 작업 진행, 측정불변성과 동시 실행 불가 안내, 허용성의 동적 상세 사유, 엔진 취소/실패 메시지는 남은 점검 대상이다. 취소 경로는 변경하지 않았으며 이번 테스트에서 실행하지 않았다. 전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
