# Fine–Gray 분석 병목 측정 — 2026-09-14

## 결론

현재 Fine–Gray 분석의 주된 비용은 결과표나 저장 처리가 아니라 `cmprsk::crr()` 내부의 분산·공분산 계산이다. 운영 코드를 변경하지 않았다. 표준오차·신뢰구간·검정과 잔차 진단에 필요한 계산을 생략하는 옵션은 적용하지 않았다.

## 전체 분석 측정

동봉 R 4.5.3, 정규 공변량 10개, 2,000행, 두 집단, 사건 코드 0/1/2, 시드 44의 합성 자료를 사용했다. `regression="fine_gray"`를 명시했으며 원인별 Cox는 실행되지 않았음을 확인했다. 실제 Fine–Gray 계수는 10개, 고유 관심사건 시점은 673개였고 세 실행 모두 수렴했다. 경고/메시지는 0개였다.

새 프로세스 3개를 순차 실행했다. 각 프로세스에서 한 번 예열하고 GC 후 프로파일러 없이 전체 `prepare_competing_risk_result()`를 한 번 측정했다. 세 번 모두 1.64초였다. CIF/Gray, 사전 검사, Fine–Gray 적합과 결과 진단을 포함하며 패키지 로딩·입력 생성·HTML·저장 파일 생성은 제외했다.

이후 같은 분석을 세 번씩 Rprof의 0.02초 간격으로 측정했다. `.Fortran` 및 그 상위 `cmprsk::crr`의 샘플 비중은 96.30% / 98.15% / 97.55%, 중앙값 97.55%였다. 두 함수는 같은 호출 스택을 포함하므로 합산하면 안 된다. 누적 샘플 시간은 3.24 / 3.24 / 3.26초이며 벽시계 실행 시간과 다르다.

## 적합 내부 단계

설치된 `cmprsk::crr` 소스를 읽고 별도 프로세스 3개에서 함수 복사본의 `.Fortran` 호출만 계측했다. 설치 패키지와 운영 함수는 수정하지 않았다. 같은 입력의 정상 함수 호출 후 계측 복사본을 호출했다. 아래 시간은 한 번의 적합 안에서 같은 루틴의 호출 시간을 합한 값의 세 프로세스 중앙값이다. 인수 준비 후 `do.call(.Fortran, ...)` 구간을 측정했으므로 순수 Fortran 연산만의 시간으로 해석하지 않는다.

| 루틴 | 한 적합의 호출 수 | 누적 시간 중앙값 |
|---|---:|---:|
| `crrvv` — 분산 계산 경로 | 1 | 1.38초 |
| `crrfsv` — 반복 적합의 목적함수·미분 계산 경로 | 3 | 0.13초 |
| `crrf` — 목적함수 평가 경로 | 3 | 0.04초 |
| `crrsr` — 잔차 계산 경로 | 1 | 0.02초 |
| `crrfit` — 기저 부분분포 위험 계산 경로 | 1 | 0.01초 |

`crrvv`는 1.39/1.38/1.38초였다. 이미 적합당 한 번 호출되므로 단순 호출 중복 제거 대상으로 볼 수 없다. `variance=FALSE`는 반환되는 분산과 잔차 등에 영향을 주어 결과 유지 조건을 충족하지 못하므로 사용하지 않았다. 다음 검토 대상은 이 계산 내부의 반복 작업이며, 연산 순서나 수치 결과를 바꾸지 않는다는 근거가 필요하다. 이번에 새로운 수치 알고리즘을 구현하지 않았다.

## 정확성 및 범위

프로파일링한 전체 결과 9건은 각 프로세스의 비프로파일 결과와 그대로 `identical(..., num.eq=FALSE)`로 일치했다. 환경 참조 정규화나 수치 허용오차를 사용하지 않았다. 별도 적합 계측 3쌍도 전체 반환값·진단·난수 상태가 일치했다. 시간 측정 안에서 결과 비교는 수행하지 않았다.

한 자료 조건의 프로파일이다. 행 수, 사건 시점 동률, 공변량 수, 검열 집단이 달라지면 비용 비중도 달라질 수 있다. 실제 HTML·PDF·Word·HWPX·Excel은 이번에 다시 생성하지 않았다. 설치 파일은 재빌드하지 않았다.

재현 자료: `output/fine-gray-profile-20260914/`의 `current.R`, `profile.R`, `phases.R`, `installed-crr.txt`, 원시 프로파일과 CSV. 운영 모듈과 사본의 SHA-256은 `9F3910F8EC6BF85A0F8346E78139BAC29FCBAA6F2AE01B9E26644F7A7A7ABD4F`로 동일하다.
