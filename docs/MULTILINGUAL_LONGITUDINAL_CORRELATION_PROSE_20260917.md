# GEE 상관구조 설명 문장 — 2026-09-17

작업상관 구조, 선택 구조, 보고 안내, 구조 비교, GEE 선택 근거, GEE 방법 표시의 6개 형식에서 상관구조 내부 코드를 번역된 표시 명칭으로 연결했다. 기존 번역 사전을 재사용한다. 알려진 5개 식별자와 앱이 작성하는 완전한 문장에만 적용하며 사용자 문자열의 일부를 바꾸지 않는다. 계산·설정·저장된 분석 원본은 변경하지 않는다.

`validate_longitudinal_correlation_prose_i18n.R`의 6형식 × 5구조 × 8언어, 총 240개 문구 검사가 통과했다. 영어 원문 유지와 임의 사용자 문자열 보존도 확인했다. 이 검사는 표시 검사이며 240개 실제 모형 적합을 의미하지 않는다.

실제 GEE/LMM/패널 고정효과의 기존 `validate_longitudinal_structural_i18n.R`와 현재·누적 5종 저장을 회귀 검사로 실행한다. 저장 입력은 `tmp/longitudinal-correlation-prose/tables.rds`, 출력은 `tmp/longitudinal-correlation-prose-exports`다. PDF 문구는 별도로 검사하며 저장 시 분석을 다시 실행하지 않는다.

다른 설명 조합·고급 옵션·실제 브라우저 왕복과 페이지별 시각 검토는 남아 있다. 메타분석과 설치본은 변경하지 않았다.

기존 실제 GEE/LMM/패널 고정효과의 8언어 검사가 모두 통과했다. 본 표 영어 유지·진단 번역·원본 라벨 보존을 확인했으며 합성 LMM의 특이 적합 경고는 유지된다. 한국어/일본어 현재·누적 HTML·PDF·Word·직접 생성 HWPX·Excel 및 PDF 캡처 문구 검사도 통과했다(두 언어 모두 현재 4쪽/누적 6쪽). 변경 파일의 `git diff --check`가 통과했다.
