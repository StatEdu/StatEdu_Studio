# Hedges’ g 입력 검증 — 2026-09-17

표본수·평균/표준편차·합동 표준편차·g/SE·신뢰구간·d·t·점이연상관·지원 입력 유형 오류 9종과 점이연상관 변환 가정 1종을 8개 언어로 입력 UI 변환기에 연결했다. 원래 계산 함수와 저장 메시지·분석 출력은 변경하지 않았다.

효과크기 오류와 조절변수 오류가 결합되는 경로도 처리했다. 전체 문장이 등록된 고정 문구들의 연속일 때만 번역하며, 알 수 없는 부분이 있으면 전체 원문을 유지한다. 사용자 문장 내부의 일부 영문을 임의로 치환하지 않는다.

`validate_meta_g_validation_i18n.R`: 실제 오류 분기 9종(합동 표준편차는 매우 작은 양수로 제곱 언더플로를 재현), 정상 점이연상관 변환 가정, 실제 정규화 함수의 복합 오류, 외부 문구 원문 보존을 8개 언어와 한국어 복귀로 검사했다. 기존 입력 UI/파서 오류 검사와 카탈로그 검사도 통과했다.

상관계수·오즈비의 검증/가정, 공통 검증 및 출판연도 동적 오류·모형 실행 오류는 후속 대상이다. 실제 브라우저 검증 및 설치본 생성은 수행하지 않았다.
