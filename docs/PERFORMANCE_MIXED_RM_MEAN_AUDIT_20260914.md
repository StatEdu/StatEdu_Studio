# 혼합 반복측정 보정 평균 중복 계산 조사

2026-09-14. 제품 코드는 수정하지 않았다. 앞서 적용한 범주형 사후 비교 일괄 회귀 이후, 시점별 보정 평균 계산에 동일 입력 재사용 기회가 남았는지 조사했다.

## 결과

`mixed_rm_descriptives()`의 집단별 보정 셀은 기존의 시점별 모형 재사용을 이미 사용하고 있다. 이번 계측은 별도로 사후 비교의 유의 순서 표기를 위해 호출하는 `mixed_rm_adjusted_time_estimate()`를 대상으로 했다.

2,000명, 4집단, Holm 사후 비교, 가정 검정 활성화 조건이다. 조건별 한 R 프로세스에서 준비 실행 후 10회 순차 분석했다. 함수 인수를 먼저 평가한 뒤 함수 실행 시간을 측정했다.

| 조건 | 분석당 호출 수 | 서로 다른 입력 수 | 평균 함수 시간 중앙값 | 계측 포함 전체 분석 중앙값 |
|---|---:|---:|---:|---:|
| 6시점, 숫자 공변량 | 30 | 30 | 0.020초 | 0.195초 |
| 12시점, 숫자 공변량 | 60 | 60 | 0.035초 | 0.530초 |
| 6시점, 숫자 + 범주형 공변량 | 30 | 30 | 0.025초 | 0.225초 |

한 분석의 첫 계측에서 반응값과 공변량 데이터프레임을 함께 직렬화해 비교했으며 동일한 인수는 없었다. 행 이름과 factor 수준 등 속성도 비교에 포함했다. 10회 함수 시간 중앙값에는 타이머 해상도의 영향이 있으며, 계측을 포함한 전체 시간은 이전의 별도 성능 실험과 직접 누적 비교하지 않는다. 첫 계측의 입력 직렬화 비용은 함수 내부 시간에서 제외했다.

## 결정

동일 입력의 계산 결과를 저장하는 캐시는 이번 대상에서 적중하지 않으므로 추가하지 않는다. 메모리와 관리 코드를 늘리는 변경도 하지 않는다.

향후 후보는 서로 다른 시점의 반응을 다중 반응 회귀로 함께 적합해 설계행렬 처리를 공유하는 것이다. 사후 대비 회귀와 보정 평균의 회귀는 반응값 자체가 다르므로 기존 대비 모형을 그대로 대신 사용할 수는 없다. 이 후보는 아직 구현·정밀도 검증·성능 검증을 마치지 않았다.

## 검증과 재현

준비 실행과 계측한 10회 분석의 전체 결과, 경고·메시지·표준출력·난수 상태는 각 조건에서 허용오차 0, 환경 객체 제외 비교를 통과했다. 결과값을 바꾸는 제품 변경은 없다.

재현 스크립트: `output/mixed-rm-mean-audit-20260914/audit.R`. 조건별 `*-times.csv`, `*-duplicates.csv`에 근거를 보관했다. 스크립트는 직전 재측정의 `measure.R` 데이터 구성 부분을 재사용한다.

현재 제품 분석 파일 SHA256:
`949E3082DAF40A1FE7CA9AB38EE8EF5D4271E8A5D09A91FCC67AE8831FB776B5`.

제품 분석·출력 코드에 변경이 없어 저장 형식 재검증, 빌드·배포는 하지 않았다.
