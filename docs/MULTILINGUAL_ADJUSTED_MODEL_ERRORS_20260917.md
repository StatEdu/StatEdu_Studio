# 조정 생존곡선 모형 조합 제한 — 2026-09-17

주변 조정 생존곡선과 시작·종료 Cox, 층화 Cox, 사용자 지정 군집 Cox, 시간가변 계수 Cox를 결합할 때의 제한 안내 4종을 8개 언어로 추가했다. 층화 모형의 별도 보고/층화 해제 안내와 군집 단위 재표집이 필요하다는 이유를 보존했다. 계산 및 검증 조건은 수정하지 않았다.

실제 Cox 모형 준비 함수에 각 조합을 전달해 오류를 발생시켰다. `validate_survival_input_errors_i18n.R`에서 기존 포함 53종(이전 의존성 모의 검사 포함), 실제 실패 콜백, 결과 초기화 및 외부 상세 보존을 8개 언어로 검사했다. 번역 커버리지 회귀 검사도 통과했다.

preflight 복합 오류와 일부 내부 모형 오류는 남아 있다. 실제 브라우저 조작이나 전체 내보내기 검사까지 완료한 것은 아니다. 결과 표·보고서 내용은 변경하지 않았으며 설치본을 만들지 않았다.
