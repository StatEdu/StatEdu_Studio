# Cox 고급 옵션 조합 제한 — 2026-09-17

스플라인과 군집 강건 분산, 시간가변 계수 변수의 공변량 포함, 시간가변 계수와 스플라인/강건 분산/시작·종료 자료, exact 방식과 시작·종료 자료/강건 분산/시간가변 계수의 제한 안내 8종을 8개 언어로 추가했다. 시간가변 계수와 시간의존 공변량 값의 구분 및 기존 제한 이유를 보존했다.

`validate_survival_input_errors_i18n.R`에 실제 잘못된 조합 8종을 추가했다. 기본/이전 옵션 오류를 포함한 총 20종의 오류와 3개 실제 실패 콜백을 8개 언어에서 검사했다. 결과 초기화 및 외부 오류 보존도 통과했다. 생존분석 알림과 번역 커버리지 회귀 검사 역시 통과했다.

계산·검증 조건·결과 표를 변경하지 않고 알림 번역만 추가했다. preflight 복합 오류, 자료의 관측 수준·고유값 부족, 경쟁위험 고유 검증 안내는 남아 있다. 실제 브라우저 조작 검증은 하지 않았다. 설치본은 만들지 않았다.
