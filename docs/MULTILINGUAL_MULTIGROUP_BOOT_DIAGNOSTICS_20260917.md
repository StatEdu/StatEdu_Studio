# 다집단 조절된 매개효과 부트스트랩 진단

2026-09-17

- 적합 유효 수, 공동 유효 수, 추론 사용 가능, 중심화 범위, 실패 횟수 등 보조표 제목과 열 제목을 UI 언어로 표시한다. 요청·적합 유효·공동 유효 수는 정수로 표시한다.
- 알려진 상태와 CI 방식, 집단 내 중심화 및 매 재표집 곱지표 재생성 설명을 번역한다. 실패 유형 5개의 코드 이름만 번역하고 등호 뒤 횟수는 보존한다. 미등록 오류 텍스트는 원문을 유지한다.
- RNG, R 버전, 분위수 유형 및 미등록 상태·CI 방식·중심화 설명은 보호한다. 분석 계산과 본 표는 변경하지 않았다.
- `scripts/validate_multigroup_boot_diagnostics_i18n.R`: 실제 진단표 생성 함수 `structural_canvas_apply_multigroup_moderation_bootstrap`에 진단 fixture를 전달했다. 충분·주의·신뢰 불가·사용자 문자열·빈 결과를 8개 언어에서 검증했다. 이 테스트는 전체 부트스트랩 재표집 실행 테스트가 아니다.
- 정수 및 백분율, TRUE/FALSE, 실패 횟수, 난수 생성기/버전/분위수 식별자, Review·Normality·한글·<&>·%s 원문 보존을 확인했다. 기존 집단별 신뢰도·HTMT 테스트도 통과했다.
- 일본어 스냅샷의 현재 4표·누적 5표를 HTML/PDF/Word/HWPX/Excel로 저장하고 내용, PDF 표지, 표 순서, Excel 시트 수를 검증했다. 저장 과정에서 재분석하지 않는다.
- 산출물: `tmp/multigroup-boot-diagnostics-i18n/`. 설치본은 만들지 않았다.

전체 다국어 감사는 아직 진행 중이다. 이번 기록은 해당 부트스트랩 진단 보조표 범위의 완료 기록이다.
