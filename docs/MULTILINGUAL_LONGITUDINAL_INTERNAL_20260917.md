# 종단 내부 오류와 고정 stop 문구 점검 — 2026-09-17

중복 대상자/시점, 무효 분산, 추정 불가 상관, 보정 GEE 계수 분산, mmrm 의존성, 비교 가능한 표준오차 없음, 비구조적 GEE 특이행렬 안내 7종을 실패 알림의 8언어 번역에 연결했다.

`validate_longitudinal_internal_i18n.R`: 중복/특이행렬은 실제 호출, mmrm은 패키지 검사 경계 모의, 나머지 4종은 원래 소스의 방어 조건에 잘못된 중간값을 넣어 검증했다. 실제 알림 콜백과 한국어 복귀도 확인했다. 전체 엔진 적합 실패를 재현한 것은 아니다.

추가로 `R/analysis_longitudinal.R`에서 문자열 리터럴을 직접 사용하는 고유 stop 문구 34개를 추출했다. 실패 알림 번역 함수에서 7개 비영어 언어 모두 영어 원문으로 남지 않음을 확인했다. 이는 알림 경계의 고정 문자열 검사이며, 동적 문장·경고·보조표에 저장된 오류·외부 패키지 메시지·브라우저 표시까지 포함하는 완료 판정이 아니다. 기존 입력 오류 검사도 통과했다.

계산·결과 표·저장은 변경하지 않았다. 다음은 결과 보조표의 오류/경고 표시 경로 점검이다. 메타분석은 제외하고 설치본은 만들지 않았다.
