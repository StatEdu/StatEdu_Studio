# 혼합 반복측정 표의 열 이름 변환 재사용 검토 — 2026-09-14

## 확인한 병목

혼합 반복측정 분석의 저장용 HTML 한 번을 생성할 때 공통 `result_column_key` 함수가 **5,570회** 호출됐다. 이 중 일반 문자열 1개를 받는 호출은 5,528회이며 실제 서로 다른 열 이름은 **30개**였다. 나머지 42회는 시험 캐시의 대상에서 제외하여 기존 함수로 처리했다. 정확히 같은 일반 열 이름의 변환 5,498회를 생략할 수 있는 조건이다.

이 함수는 열 이름의 제곱 기호를 숫자로 바꾸고, 소문자 변환과 영숫자 이외 문자 제거를 수행한다. 표의 셀 내용·주석 열 판정·스타일 계산 등에서 반복 사용된다.

## 시험 결과

운영 코드를 변경하지 않고 계측 스크립트 안에서만 함수를 대체했다. 후보는 HTML 생성 한 번마다 새로 만든 최대 256개 변환값 목록을 사용했다. 일반적인 속성 없는 단일 문자열만 재사용하고 벡터·결측·속성 있는 입력 등은 기존 함수를 호출한다. 다음 렌더링에는 이전 목록을 넘기지 않았다.

관측치 2,000개·집단 4개·시점 6개·연속 공변량 1개인 기존 분석 결과를 읽어 HTML만 생성했다. 분석은 다시 실행하지 않았다. 번들 R 4.5.3, 한국어 설정, 독립 프로세스 3쌍을 순차 실행했으며 두 번째 쌍은 순서를 반대로 했다.

| 버전 | 최초 3회(초) | 반복 3회(초) | 최초 중앙값 | 반복 중앙값 |
|---|---|---|---:|---:|
| 기존 | 1.20, 1.14, 1.14 | 0.78, 0.56, 0.53 | 1.14 | 0.56 |
| 시험 후보 | 1.04, 1.06, 1.04 | 0.46, 0.44, 0.46 | 1.04 | 0.46 |

이 조건에서 반복 HTML 생성 중앙값은 **약 17.9% 감소**했다. 최초 중앙값은 약 8.8% 감소했다. 기존 첫 회차의 반복 시간이 상대적으로 컸으며, 세 쌍 모두 후보가 빨랐다. 실제 화면 지연이나 앱 로딩, PDF/문서 변환의 개선율을 측정한 것은 아니다.

## 동일성 확인

- 세 쌍 모두 변경 전/시험 후보의 HTML 전체, 경고·메시지·표준 출력·난수 상태가 정확히 일치했다.
- 각 프로세스의 최초/반복 출력도 동일했고, 원본 분석 결과의 직렬화 값은 렌더링 전후 같았다.
- 실제 렌더링의 함수 입력을 별도로 수집해 5,570회와 일반 열 이름 30개를 확인했다. 모든 호출이 단일 일반 문자열이라는 초기 계측 가정은 틀렸고, 42개 우회 입력을 별도로 저장했다. 후보는 처음부터 해당 입력을 기존 함수로 처리하고 있었다.

## 적용 판단

**이번 단계는 후보 시험이며 운영 적용은 아직 하지 않았다.** `result_column_key`는 R 코드의 21개 호출 위치에서 사용되는 공통 함수다. 한 분석의 HTML 안에서만 재사용한 시험 결과를 전역 캐시에 그대로 확대해서는 안 된다.

다음 구현에서는 렌더링 단위로 범위를 제한하거나, 지속 캐시를 사용할 경우 로케일과 문자열 인코딩을 포함한 동일성·무효화 조건을 검증해야 한다. 특히 소문자 변환과 문자 분류는 로케일 영향을 받는다. 벡터·결측·이름/클래스 속성·다국어 입력의 기존 결과를 유지하고, 캐시 용량과 새로운 열 이름이 계속 들어오는 경우도 확인해야 한다.

적용 시 회귀·상관·공분산·반복측정 등 공통 표 경로와 현재/누적 HTML·PDF·Word·HWPX·Excel을 함께 검증한다. 이번에는 실제 출력 코드를 수정하지 않아 파일 변환을 다시 실행하지 않았다.

## 재현 자료

`output/mixed-rm-column-key-review-20260914/`: `measure.R`, `verify.R`, 3쌍 CSV/RDS, 열별 호출 수 CSV, 우회 입력 RDS, 검증 로그.

측정 시 `R/result_table_ui.R` SHA-256: `CB9826D8B401CEE96352E4920C5F7EFB580076B0F84C9A70C0ACD6FB7C22BD55`.
