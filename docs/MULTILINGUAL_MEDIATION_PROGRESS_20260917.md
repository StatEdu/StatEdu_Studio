# 매개·조절 부트스트랩 진행 세부 문구 — 2026-09-17

- `mediation_moderation_bootstrap_job_progress()`의 추가 6개 언어 표시를 구현했다. 기존 영어/한국어 표시 경로는 유지한다.
- 준비, 재표집, 통계 계산, 저장, 완료 및 알 수 없는 단계의 안내를 번역했다. 반복 수, 모형 순번, 변수명, 처리 속도, 경과/잔여 시간 및 잔여 시간 추정 중 안내를 포함한다.
- 진행 캐시, 역행 방지, 속도 표본의 중앙값 및 ETA 계산은 변경하지 않았다.
- `scripts/validate_mediation_progress_i18n.R`: 실제 진행 RDS를 읽는 함수를 8개 언어 × 7단계 × ETA 가능/불가능 상태로 검사했다(112조합). 변수명 `Review 사용자 <&>` 보존, 수치/진행률, 비재표집 단계 ETA 제거, 오래된 상태에 의한 역행 방지를 확인했다.
- 기존 사용자 캔버스 4개 진행 카드와 공통 다국어 검사도 통과했다.
- 로그: `tmp/mediation-progress-{validation,coverage}.log`, `tmp/custom-canvas-progress-validation.log`.
- 실제 분석 프로세스와 브라우저 통합 검증은 별도로 남아 있다. 분석 결과표/내보내기는 변경하지 않아 저장 검사를 재실행하지 않았다.

전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
