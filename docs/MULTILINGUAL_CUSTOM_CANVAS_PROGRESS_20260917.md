# 사용자 매개·조절 모형 작업 화면 — 2026-09-17

- 작업 시작/진행 제목, 예정 재표집 수, 결과 불러오기 및 결과표·그림·캔버스 구성 안내를 UI 언어로 표시한다.
- 중단 버튼 문구도 공통 다국어 사전을 사용한다. 작업 ID, 진행률, 결과 로딩·적용 로직은 변경하지 않았다.
- `scripts/validate_custom_canvas_progress_i18n.R`: 실제 화면 생성 표현식 4개를 8개 언어로 실행해 제목/단계 번역, 예정 횟수, 전달된 진행 내용, 중단 버튼 ID를 확인했다(32조합).
- 공통 다국어 사전, 구문 및 diff 공백 검사 통과. 로그: `tmp/custom-canvas-progress-{validation,coverage}.log`.
- 진행 세부 내용은 `mediation_moderation_bootstrap_job_progress()`에서 전달받는다. 이번 검증은 전달값을 보존하는지 확인한 것이며 그 함수의 전체 번역 검증은 별도로 필요하다.
- 실제 브라우저에서 작업 시작·취소 및 파일 로딩을 수행하는 통합 검증은 이번 범위에 포함하지 않았다. 분석 출력/저장 내용은 변경하지 않아 내보내기 검사를 재실행하지 않았다.

전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
