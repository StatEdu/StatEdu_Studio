# Egger 보조표 다국어 — 2026-09-17

메타분석 결과 화면 제목과 Egger 보조표의 열 이름, 계산 불가 사유 3종, 유의/비유의 해석을 8개 언어의 번역 테이블에 연결했다. 미등록 외부 오류는 원문을 보존한다. 본표 영어와 통계 계산은 변경하지 않았다.

`validate_meta_egger_i18n.R`은 실제 최소 연구 수/동일 표준오차 오류와 정상 적합, 모의 절편 실패, p=.01/.05/.5 해석 경계, 사용자 문자열 및 수치 보존, 한국어 복귀를 검사한다. `validate_meta_warning_exports_i18n.R`은 실제 결과 UI에 보조표 변형을 추가한 시험 스냅샷으로 8개 언어 렌더링과 한국어·일본어 현재/누적 HTML/PDF/Word/HWPX/Excel을 검사한다. `validate_meta_warning_pdf.py`는 PDF 텍스트와 편집 문서의 그림 포함을 확인한다. 별도 카탈로그 회귀 검사도 수행한다.

실제 브라우저 조작 및 문서 페이지별 시각 검토는 포함하지 않는다. 설치본을 만들지 않는다. 다음 확인 대상은 연구 내 상관 민감도, 논문 단위 순차 제외, Trim-and-fill, 연구별 효과크기 보조표의 다국어 경로다. 전체 앱 완료를 의미하지 않는다.

위 자동 검사는 모두 통과했다. 산출물: `tmp/meta-warning-i18n`.
