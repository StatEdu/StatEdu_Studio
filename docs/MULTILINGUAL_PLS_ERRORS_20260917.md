# PLS 모형 제약 오류 안내 — 2026-09-17

- PLS 통제변수, 모수 제약, 순환 경로, 중복 지표, 일반 지원 불가, 데이터에 없는 지표, 데이터 열 불일치 안내를 추가 6개 언어로 번역했다. 기존 영어/한국어 경로는 유지한다.
- 알려진 오류의 상세 구분자 뒤에 있는 변수명/수정 요소는 원문으로 보존한다. 측정변수 누락 안내의 변수 목록도 보존한다.
- `scripts/validate_pls_errors_i18n.R`: 엔진 오류 형식의 7개 시험 메시지를 8개 언어로 검사했다. 이름 `Review 사용자 <&> %s, x.1`과 알 수 없는 오류의 원문 보존을 확인했다.
- 표본설계 오류 회귀 검사, 공통 다국어 사전 검사 및 diff 공백 검사 통과.
- 로그: `tmp/pls-errors-{validation,coverage}.log`, `tmp/sampling-design-errors-validation.log`.
- 이번 범위는 오류 표시 함수다. 실제 PLS 엔진에서 7개 오류를 각각 유발하거나 브라우저 알림을 확인하지는 않았다. 결과표/내보내기는 변경하지 않아 저장 검사를 재실행하지 않았다.

선택 경로·수정지수 등 나머지 오류와 실제 UI 통합 검증은 남아 있다. 전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
