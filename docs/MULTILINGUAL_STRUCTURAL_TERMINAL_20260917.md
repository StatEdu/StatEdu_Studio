# 구조효과·PLS 종료 알림 다국어 — 2026-09-17

- 구조효과/다집단 잠재조절 및 PLS/PLSc의 중단·완료·실패 알림을 UI 언어로 표시한다. 구조효과 일부 성공 시 정상 결과 유지 안내도 포함한다.
- PLS 완료 알림의 유효/요청 재표집 수와 7개 실패 유형(시간 제한, 추정 실패, 비수렴, 허용 불가 해, 통계량 요건 실패, 실행 실패, 취소)을 번역한다.
- 유효율 80% 미만의 추론값 표시 억제 안내 및 결과표 갱신 안내를 번역한다. 원래 계산/상태 판정은 유지한다.
- 알림 문자열 생성만 분리했다. 저장된 엔진 오류 원문, 부트스트랩 결과, 작업 중단·정리 및 결과 갱신 로직은 변경하지 않았다.
- `scripts/validate_structural_terminal_i18n.R`: 8개 언어에서 3종 모형명 × 4종 상태, PLS 추론 가능/불가능 상태, 7개 실패 유형 숫자와 오류 원문 보존을 확인했다.
- 기존 SEM/PLS 진행 표시 검사, 다국어 사전 검사, 구문 및 diff 공백 검사 통과.
- 로그: `tmp/structural-terminal-{validation,coverage}.log`, `tmp/structural-progress-details-validation.log`.
- 이번 검증은 실제 알림 생성 함수 단위다. 실제 백그라운드 프로세스의 성공·부분 실패·취소를 브라우저에서 유발하는 통합 검증은 수행하지 않았다.
- 결과표/내보내기는 변경하지 않아 저장 형식 검사를 재실행하지 않았다.

전체 다국어 완료를 의미하지 않는다. 작업 초기 알림, 상세 엔진 오류 및 브라우저 통합 경로의 추가 점검이 남아 있다. 설치본은 생성하지 않았다.
