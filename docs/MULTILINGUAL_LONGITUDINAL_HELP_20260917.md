# 종단 설정 설명문·점검 옵션 — 2026-09-17

가중치 비활성화·혼합모형 권고·미선택·무가중치·GEE/패널 가중치 해석·일반 대상 모집단 안내와 IPW 보조변수 설명 등 8개 문구를 8언어 공통 사전에 연결했다. 한국어 원문은 기존 코드에서 추출해 유지했다. 계산·결과표는 변경하지 않았다.

`validate_longitudinal_help_i18n.R`에서 9개 생성 화면 상태 × 8언어 72개를 확인했다. 그중 선택된 가중치와 무가중치 안내 1개는 현재 정규화 로직이 자동으로 바꾸는 상태이므로 시험에서 이전/전환 중 상태를 구성해 검사했다. 일반 대상 모집단 안내는 사전에 포함하되 현재 지원 모형의 화면에서는 도달하지 않아 별도 화면 검증에 포함하지 않았다.

모형별 가정 점검 이름, 결측 처리·가중치 유형·절단 상세 안내도 8언어에서 확인했다. Hausman FE vs RE는 통용 약어 표기로 유지될 수 있다. 생성 HTML의 문구 포함과 상태 객체 불변 검사는 통과했다. 실제 브라우저 이벤트 검증은 아니다.

기존 옵션 검사(16개 묶음 × 8언어) 및 선택값·변수 배치 검사(생성 화면 56개)도 통과했다. 설정 화면만 변경하여 결과표·5종 저장은 재검사하지 않았다. 다른 설명·상태 조합과 실제 브라우저 왕복은 남아 있다. 메타분석과 설치본은 변경하지 않았다.

별도 계산기 작업에서 준비한 `scripts/fill_calculator_errors_i18n.py`를 검토·실행하여 calculator.error.* 4개 키를 8언어 공용 사전에 병합했다. 계산기 동작 검증은 담당 작업 범위이며 이 문서의 종단 검증 수치에 포함하지 않는다. 병합 완료 전달 메시지는 자동 승인 검토에서 차단되어 전송되지 않았다.
