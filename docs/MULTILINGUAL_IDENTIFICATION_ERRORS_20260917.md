# 모형 식별성 안내 다국어 — 2026-09-17

- 단일/두 지표, 미측정 잠재변수, 고차요인, 혼합 측정, 고정 잔차분산, 중복 경로/공분산의 고정 안내 12종을 추가 6개 언어로 번역했다.
- 식별성 점검 실패 및 경고 제목을 UI 언어로 표시한다. Element에 저장된 변수/요인명은 유지한다.
- 교차적재의 동적 요인 목록과 미등록 코드 메시지는 원문을 유지한다. 이 경로의 완전한 다국어 지원은 후속 점검이 필요하다.
- `scripts/validate_identification_errors_i18n.R`: 진단 엔진 소스의 실제 고정 문구 12개를 8개 언어에서 검사했다. 오류 조합과 경고 알림 생성, Element `Review 사용자 <&> %s` 및 동적/미등록 메시지 보존을 확인했다.
- 특이행렬 오류 회귀 검사, 공통 다국어 사전 및 diff 공백 검사 통과.
- 로그: `tmp/identification-errors-{validation,coverage}.log`, `tmp/singular-errors-validation.log`.
- 각 진단을 실제 모형으로 발생시키거나 브라우저에서 확인한 검증은 아니다. 결과표/내보내기는 변경하지 않아 저장 검사를 재실행하지 않았다.

전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
