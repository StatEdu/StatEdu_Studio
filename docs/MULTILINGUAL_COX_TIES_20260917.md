# Cox 동률 사건 요약

2026-09-17

- 소스 번역 후보 검사를 재실행했을 때 기존 ANCOVA 후보는 남지 않았다. 정적 검사 결과이며 모든 동적 분기 완료를 뜻하지 않는다.
- Cox 동률 사건 요약의 열 제목 5개, 요약 제목, 방법 사전 지정 및 최소 p값에 의한 선택을 하지 않는다는 설명 등 7개 항목을 추가 6개 언어에 보완했다. 기존 보조표 번역 경로를 사용한다.
- `validate_cox_ties_i18n.R`: 기존 생존분석 fixture의 시간을 반올림해 동률을 만들고 Efron/Breslow/Exact Cox 모형을 실제 적합했다. 각 방법×8개 언어에서 요약 제목·열·설명 번역, 사건 수·비율·방법 이름 보존, 본표 셀 동일성을 확인했다.
- 검사 패널은 제품에서 사용하는 동일한 제목·표·설명 렌더러로 구성했다. 전체 Cox 화면 모든 보조표를 검사한 것은 아니다.
- 일본어 동률 요약 스냅샷의 현재 3표·누적 4표를 HTML/PDF/Word/HWPX/Excel로 저장했다. 내용, PDF 표지, 표 순서 및 Excel 시트 수 검증 통과. 저장 시 재분석하지 않는다.
- 산출물: `tmp/cox-ties-i18n/`, `tmp/cox-ties-test.log`, `tmp/cox-ties-exports.log`.
- 설치본은 생성하지 않았다. 생존분석의 나머지 자료 포함·제외 및 영향력 진단은 후속 점검 대상이다.
