# 종단 REML·예측 제한 안내 — 2026-09-17

REML 비유한 도함수, 반복 LMM 계수 추론, 반복 LMM 예측구간, 보정 GEE 예측 표준오차, 행별 모형 가중치, 반복 REML 추가 옵션 제한, 알 수 없는 모형 유형의 오류 7종을 8개 언어의 실행 실패 알림에 연결했다.

`validate_longitudinal_limits_i18n.R`에서 5종은 실제 함수 호출, REML 도함수는 초기 최적화 반환 경계만 모의 처리한 함수 복제, 계수 추론은 실제 소스의 조건문에 잘못된 계수 행렬을 넣어 검사했다. 8언어 및 한국어 복귀, 실제 알림 콜백의 결과 초기화·유형·시간, 외부 원문 보존을 확인했다. 지원되는 LMM/GEE 예측 어댑터는 원래 lm/glm 예측값과 동일했다. 기존 입력/실행 환경 오류 검사도 통과했다.

실제 모형 추정 실패를 전부 재현하거나 브라우저 알림을 표시한 검사는 아니다. 계산·본표·보조표·저장은 변경하지 않았다. 남은 짧은 내부 오류·Shiny 입력 검증 문구는 추가 점검 대상이다. 메타분석은 제외하고 설치본은 만들지 않았다.
