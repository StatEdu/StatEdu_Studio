# 표본설계 오류 안내 다국어 — 2026-09-17

- 구조방정식 캔버스의 표본설계 미지정, 군집/다층, 복합표본, 종단/반복측정 및 일반 지원 불가 안내를 추가 6개 언어로 번역했다. 기존 영어 원문과 한국어 경로는 유지한다.
- 표본설계 지원 여부 판정이나 분석 엔진은 변경하지 않았다. 오류를 사용자에게 전달하는 함수만 수정했다.
- `scripts/validate_sampling_design_errors_i18n.R`: 실제 표본설계 검사 함수에서 발생시킨 4개 오류와 일반 문구를 8개 언어에서 확인했다. 지원되는 독립 횡단자료는 계속 통과한다.
- 알려지지 않은 엔진 오류의 변수명·기호·숫자 원문 보존도 확인했다.
- 공통 다국어 사전, 구문 및 diff 공백 검사 통과. 로그: `tmp/sampling-design-errors-{validation,coverage}.log`.
- 결과표/내보내기는 변경하지 않아 저장 검사를 재실행하지 않았다. 실제 브라우저 오류 알림 통합 검증은 별도로 남아 있다.

PLS 모형 제약, 선택 경로, 수정지수 등 나머지 오류 분기는 후속 점검 대상이다. 전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
