# 경쟁위험 입력 오류 — 2026-09-17

경쟁위험 회귀 공변량 미선택, Fine-Gray 전용 검열 분포 층화, 층화 변수 부재/역할 중복, 관심 사건 코드 개수, 층화 수준의 최소/최대 조건 등 7종을 8개 언어로 추가했다. 기존 실패 알림 번역 함수에 연결했으며 사건 매핑·분석 검증 조건·계산은 유지했다.

실제 경쟁위험 준비 함수에 공변량 누락, 부적합 층화 변수, 두 관심 사건, 1개/21개 층화 수준의 자료를 전달해 오류를 발생시켰다. `validate_survival_input_errors_i18n.R`에서 기존 포함 38종의 실제 오류와 3개 실패 콜백을 8개 언어로 검증했다. 결과 초기화, 외부 오류 상세 보존, 번역 커버리지도 통과했다.

사전 점검 복합 오류, Fine-Gray 추정 가능한 설계열 부족 및 필수 패키지 안내 등은 후속 점검 대상이다. 실제 브라우저 조작이나 전체 분석 출력 내보내기 검증은 하지 않았다. 설치본을 만들지 않았다.
