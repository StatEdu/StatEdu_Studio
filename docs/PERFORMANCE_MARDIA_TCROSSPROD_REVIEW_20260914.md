# Mardia 전치 행렬 생성 제거 후보 — 2026-09-14

블록 합산과 별개로 거리 행렬 생성식만 `tcrossprod(centered %*% inv_cov, centered)`로 바꾸는 후보를 검사했다. 전체 거리 행렬과 기존 `mean(distances^3)`, 대각·첨도 계산은 유지했다. 측정한 입력의 원시 결과는 같았지만 속도·메모리 이득이 부족해 제품에 반영하지 않았다.

## 결과와 비용

500/1,500/3,000행 × 세 시드의 9개 입력에서 원시 왜도·왜도 χ²·p값·첨도·첨도 z·p값이 모두 `identical(..., num.eq=FALSE)`로 같았다. 표시 표·정규성 판정·경고/메시지 클래스와 문자열 및 순서·난수 상태도 같았다. 거리 행렬 대각의 수치도 이름 속성을 제외한 비교에서 같았다. 전체 비대각 원소를 직접 비교한 것은 아니며 다른 BLAS·플랫폼·입력에 대한 일반적인 정확 동일성을 주장하지 않는다.

| 3,000행 검사 | 기존 | 후보 |
|---|---|---|
| 시간 3회(초) | 0.47 / 0.54 / 0.47 | 0.56 / 0.47 / 0.56 |
| 시간 중앙값(초) | 0.47 | 0.56 |
| 누적 R 할당 bytes | 145,545,624 | 145,257,576 |
| 최대 단일 할당 bytes | 72,000,048 | 72,000,048 |

세 번 중 두 번 느렸다. 누적 할당 감소는 288,048 bytes(약 0.29MB, 약 0.2%)로 전치 입력 행렬 하나 정도에 불과했다. 거리 행렬과 세제곱 임시 배열의 큰 할당은 남는다. 최대 동시 메모리나 프로세스 RSS를 측정한 것은 아니다. 측정 시간은 전체 요인분석이 아니라 검사 함수와 비교용 원시 값 수집의 시간이다.

## 방법

- 번들 R 4.5.3의 새 프로세스 3개를 순차 실행했다. 프로세스별 시드는 930/931/932이고, 공통 정규 잠재값과 독립 정규 오차로 변수 12개를 생성했다. 각 검사 전 시드 718로 초기화했다.
- 현재 제품 함수의 사본을 계측해 기존 최종 표와 원시 통계량·대각을 함께 반환했다. 후보는 계측된 함수에서 거리 행렬 생성식 한 곳만 변경했다. 블록 계산 후보는 사용하지 않았다.
- 각 크기에서 양쪽 준비 실행과 정확 비교 후, 마지막 3,000행 입력으로 GC와 시간 측정을 수행했다. 두 번째 프로세스는 후보를 먼저 측정했다. 세 시간은 같은 입력 반복이 아니라 세 시드의 짝 비교다.
- 시간 측정 뒤 별도 GC 및 Rprofmem 실행으로 할당량을 기록했다. 각 방식 내부의 준비/시간/메모리 계측 결과가 정확히 같음을 확인했다. 다른 R 타이밍 작업은 동시에 실행하지 않았다.
- 이전 블록 후보 보고서의 시간과 직접 비교해 개선율을 산출하지 않았다. 로딩·자료 생성·화면·내보내기와 후속 ML/PA 적합은 측정 범위 밖이다.

기준 소스·계측 함수·후보·실행 코드·원시 값 RDS·비교/시간 CSV·메모리 할당 로그는 `output/mardia-tcrossprod-review-20260914/`에 보관했다. 제품 소스와 출력 형식은 변경하지 않았다. 기존 회귀 검증·파일 변환·설치 파일 빌드는 재실행하지 않았다. 이 행렬 곱 교체 후보의 검토는 종료한다.
