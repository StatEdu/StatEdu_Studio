# 교차적재 동적 안내 — 2026-09-17

- 알려진 교차적재 메시지의 고정 접두/접미부만 식별하여 요인 목록을 그대로 보존하고 한국어 및 추가 6개 언어로 안내를 표시한다. 영어 표시와 진단 판정은 유지한다.
- 정확한 메시지 형식에 맞지 않으면 원문을 반환한다. 요인 목록을 쉼표로 분해하거나 다시 번역하지 않는다.
- `scripts/validate_cross_loading_i18n.R`: 두 요인에 공유된 지표가 있는 실제 캔버스 모형을 진단 함수에 전달해 교차적재 경고를 생성했다. 8개 언어에서 요인 목록과 지표명, 메시지 조합, 미등록 형식 원문 보존을 확인했다.
- 요인명에는 쉼표, 마침표, 한국어, `<&>`, `%s`를 포함했다.
- 기존 식별성 안내 12종 검사와 공통 다국어 사전, diff 공백 검사 통과.
- 로그: `tmp/cross-loading-{validation,coverage}.log`, `tmp/identification-errors-validation.log`.
- 모형 추정이나 브라우저 알림 통합 검증은 이번 범위에 포함하지 않았다. 결과표/내보내기는 변경하지 않아 저장 검사를 재실행하지 않았다.

전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
