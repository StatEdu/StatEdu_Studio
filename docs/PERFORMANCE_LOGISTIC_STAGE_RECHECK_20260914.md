# 로지스틱 회귀 단계별 재측정 — 2026-09-14

현재 병목은 이항의 VIF 보조회귀와 다항의 모형 적합·계수 표용 공분산 계산이다. 최근 순서형 누적확률 개선 이후의 소스로 재확인했으며, 이번에는 운영 코드를 변경하지 않았다. 이전에 제외한 Hessian 재작성이나 Brier 계산 후보를 다시 적용할 근거는 얻지 못했다.

## 측정 결과

50,000행, 연속 예측변수 6개를 2개씩 추가하는 3단계 모형이다. 새 R 프로세스 3개를 순차 실행했다. 각 프로세스에서 준비 실행 후 경과 시간을 측정하고 별도로 Rprof를 실행했다.

| 입력 → 실제 모형 | 전체 3회(초) | 전체 중앙값 | 적합 단계 | VIF | 예측 성능 지표 |
|---|---|---:|---:|---:|---:|
| 이항 → 이항 | 1.18 / 0.78 / 0.78 | 0.78 | 0.36 | 0.33 | 0.03 |
| 순서형 → 다항 자동 전환 | 4.65 / 4.45 / 4.37 | 4.45 | 3.48 | 0.33 | 0.14 |
| 명목형 → 다항 | 3.88 / 3.85 / 3.82 | 3.85 | 3.31 | 0.34 | 0.12 |

단계 시간은 각 실행의 3개 모형 호출을 합산한 뒤 구한 중앙값(초)이다. 다항 계수 표 단계는 명목형 1.53초, 순서형 입력 1.71초이며 **적합 단계에 포함되므로 별도로 더하면 안 된다**. 순서형 입력의 평행선 검정은 0.40초였다. 전체 시간에는 결과 준비·조건 수집·공식 환경 정규화가 포함되며, 패키지 로딩·입력 생성·화면 렌더링·내보내기는 제외했다. 함수 래퍼의 계측 비용도 포함한다.

명목형 두 번째 Rprof에서 계수 표의 `summary → vcov → multinomHess` 경로가 전체 표본의 약 41%였다. `logistic_multinom_coef_table()`의 summary 호출은 한 번이다. 표본 비율은 실제 경과 시간 비율과 같지 않으며 하위 호출과 중복된다.

## 해석 및 다음 범위

- 이항에서는 VIF가 전체 시간의 상당 부분을 차지하지만, 기존 결과 재사용은 이미 적용되어 있다. summary 생략 후보도 앞서 검토했으므로 신규 개선으로 계산하지 않는다.
- 다항에서는 예측 지표보다 적합·공분산 계산이 크다. 이전 Hessian 작업 행렬 후보는 전체 이득의 일관성과 유지보수 비용 때문에 미채택되었고, 이번 재측정만으로 그 결정을 바꾸지 않는다.
- 이번 순서형 입력은 3범주이며 기존 판정에 따라 다항으로 전환된다. 최근 적용한 **50,000행 이상·20범주 이상 순서형 누적확률 경로를 실행하지 않는다**. 해당 개선 효과는 별도 `PERFORMANCE_ORDINAL_CUMULATIVE_PROBABILITY_20260914.md`의 검증을 참조한다.
- 이전 측정과 이번 측정은 동일 시점의 교대 비교가 아니다. 환경 변동과 계측 차이가 있으므로 과거 시간과의 차이를 성능 개선 또는 회귀로 해석하지 않는다.
- 다음 검토에서는 이 경로의 재작성 반복보다 다른 분석의 대용량 반복 계산을 측정해 새 후보를 찾는다. 아직 확인하지 않은 후보의 개선율은 제시하지 않는다.

## 결과 동일성 및 재현

각 자료 유형에서 준비/시간 측정/프로파일 결과가 일치했다. 저장한 새 프로세스 3개 결과끼리도, 이전 프로파일의 동일 입력 결과와도 전체 반환값·경고/메시지 문자열·표준출력·난수 상태가 `identical(..., num.eq=FALSE)`로 일치했다. 비교를 위해 공식 환경만 `.GlobalEnv`로 통일했으며 수치 허용오차는 사용하지 않았다. 조건 객체의 호출 환경이나 장치 상태까지 검증한 것은 아니다.

`output/logistic-stage-recheck-20260914/profile.R`에 측정 코드, `summarize.R`에 교차 비교 및 단계 집계 코드를 보관했다. `n50000/`에 RDS, Rprof, CSV, 상위 폴더의 `verification.txt`에 확인 결과가 있다. 요약 스크립트 첫 실행은 상속 로캘 경고가 발생해 `LC_ALL`과 `LANG`을 `English_United States.utf8`로 설정한 뒤 재실행했으며, 최종 검증에는 경고가 없었다.

운영 소스와 출력 내용·형식은 수정하지 않았으므로 내보내기 재생성 및 설치 파일 빌드는 수행하지 않았다. 메모리 사용량은 이번에 측정하지 않았다.
