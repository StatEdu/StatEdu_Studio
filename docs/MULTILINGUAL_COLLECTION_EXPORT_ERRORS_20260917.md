# 누적 결과 내보내기 오류 안내 — 2026-09-17

표/그림 없음, 표 없음, Excel 구성 실패, PDF 브라우저 없음, PDF 저장 경로 실패, Office 도표 변환 브라우저 없음, PDF 변환 실패 등 7종의 안내를 8개 언어로 추가했다. HTML/PDF/Excel/Word 누적 결과 저장의 오류 알림 경계에 연결했다. 영어 본문과 정확히 일치하는 앱 문구만 번역하고 PDF 오류의 고정 첫 줄 뒤 상세 로그 및 외부 오류는 원문을 유지한다. 분석 결과 본문·파일 작성 로직·HWPX 제어는 변경하지 않았다.

검증 및 제한:
- `validate_collection_export_errors_i18n.R`: 저장 함수 경계에서 오류를 주입하여 8개 언어 × 4개 형식의 Shiny 오류 알림을 검사했다. 7종 문구, PDF 상세 로그, 외부 오류 보존 및 결과 스냅샷 불변 검사가 통과했다. 실제 파일 변환 검사는 아니다.
- 결과 이력 흐름 및 번역 커버리지 회귀 검사도 같은 격리 환경에서 통과했다.
- 검사 시점에 `R/app_bootstrap.R`가 없는 `R/result_hwpx_native.R`를 참조하여 정상 전체 모듈 로딩은 실패했다. 이번 작업에서 추가한 참조가 아니므로 관련 파일을 수정하지 않았다. `tmp/run-without-pending-hwpx.R`에서 이 모듈만 로딩 목록에서 제외하여 이번 범위를 검증했다. 전체 앱 검증 통과로 간주하지 않는다.
- 개별 분석 화면 오류 알림은 이번 연결 범위에 포함하지 않았다. 네이티브 창·전체 내보내기 실제 실행 검증 및 설치본 생성은 하지 않았다.
