# 생존분석 내부 오류 안내 — 2026-09-17

자료 미입력, 설정 리스트 형식, 시간가변 Cox 기본/시간 상호작용 계수 식별 실패, Cox 스플라인 설계행렬 재구성 실패의 고정 오류 4종을 8개 언어로 추가했다. UI 오류 변환기에만 연결했으며 계산·검증 조건·본표·보조표·보고서 내용은 변경하지 않았다. 알려지지 않은 외부 오류 상세는 원문을 유지한다.

`validate_survival_internal_errors_i18n.R`에서 실제 오류 분기 4종을 실행했다. 계수/행렬 오류는 불일치하는 시험 모형 객체로 해당 방어 조건을 발생시켰다. 또한 `R/analysis_survival.R` 구문에서 추출한 고유 고정 문자열 `stop` 메시지 50개가 영어에서는 그대로이고 나머지 7개 언어에서는 번역되는지 전수 검사했다. 동적 문구·패키지 내부 오류·다른 파일까지 포괄하는 완료 지표는 아니다.

기존 `validate_survival_input_errors_i18n.R`의 53종 오류/실패 콜백 검사와 `validate_multilingual_coverage.R`도 통과했다. 브라우저에서 각 내부 실패를 재현한 것은 아니다. 설치본은 만들지 않았다.
