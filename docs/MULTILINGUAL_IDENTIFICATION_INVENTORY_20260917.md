# 식별성 진단 코드 목록 대조 — 2026-09-17

- 진단 함수의 고정 문구 목록을 대조하여 `structural_cycle`의 추가 6개 언어 및 한국어 번역, `duplicate_covariance`의 한국어 번역 누락을 보완했다.
- `scripts/validate_identification_inventory_i18n.R`는 엔진 소스에서 고정 문구 13종을 추출해 8개 언어에서 검사한다. 목록 크기도 검사하여 이후 항목 추가 시 검증 범위 재확인을 요구한다.
- 상호회귀 경로 및 반대 방향으로 중복된 공분산 경로를 실제 진단 함수에 전달해 해당 오류 발생과 안내 번역을 확인했다.
- 고정 문구 13종 검사, 별도 교차적재 동적 메시지 검사, 공통 다국어 사전 및 diff 공백 검사 통과.
- 로그: `tmp/identification-inventory-{validation,coverage}.log`, `tmp/cross-loading-validation.log`.
- 이 결과는 해당 식별성 진단 함수의 문구 목록을 다룬다. 모든 진단을 실제 모형 추정/브라우저에서 실행했거나 전체 앱 번역을 완료했다는 의미는 아니다.
- 결과표/저장 내용은 변경하지 않아 내보내기 검사는 재실행하지 않았다. 설치본은 생성하지 않았다.
