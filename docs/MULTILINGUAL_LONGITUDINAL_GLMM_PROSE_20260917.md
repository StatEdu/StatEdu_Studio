# GLMM 분포 설명 — 2026-09-17

GLMM 선택 근거 문장에 들어가는 알려진 분포 식별자를 UI 언어의 분포명으로 표시하도록 수정했다. 기존 분포명 사전을 재사용하며 알려지지 않은 식별자는 그대로 보존한다. 계산·설정 값과 분석 원본은 변경하지 않는다.

`validate_longitudinal_glmm_prose_i18n.R`에서 이항·Poisson·음이항·감마의 4경로 × 8언어 32개 보조표 검사를 통과했다. 각 표의 선택 근거와 민감도 권고 3개, 분포명 포함, 원본 표 보존을 확인했다. 민감도 권고는 기존 번역으로 정상 표시되어 추가 번역하지 않았다. 임의 분포 식별자 보존도 검사했다. 이 검사는 표시 검사이며 32개 실제 적합을 의미하지 않는다.

실제 모형의 보고 점검표 회귀 검증은 `validate_longitudinal_checklist_models_i18n.R`로 수행한다. 이 스크립트에는 GEE/LMM/패널 및 GLMM 이항·Poisson·감마·음이항·확률기울기와 빈 결과를 포함한 11개 경로가 있다. 저장 검증은 캡처한 표를 `tmp/longitudinal-glmm-prose/tables.rds`에서 읽어 한국어/일본어 현재·누적 HTML·PDF·Word·직접 생성 HWPX·Excel로 출력한다. 경로는 `tmp/longitudinal-glmm-prose-exports`이며 저장 시 분석을 재실행하지 않는다. 저장 검사의 본 표는 영어 계수 시험 표다.

실제 브라우저 왕복·페이지별 시각 검토 및 다른 설명·옵션 조합은 남아 있다. 메타분석과 설치본은 변경하지 않았다.

보고 점검표 11경로 × 8언어 88개 검사가 통과했다. 합성 모형의 특이 적합·음의 고유값 수렴 경고와 공통 시점 부족 경고는 유지되며 검증 통과는 수렴 보장을 의미하지 않는다. 한국어/일본어 현재·누적 5종 저장 및 PDF 캡처 문구 검사도 통과했다(두 언어 모두 현재 3쪽/누적 4쪽). 변경 파일의 `git diff --check`가 통과했다.
