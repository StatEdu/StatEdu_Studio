# CFA/SEM 비동기 부트스트랩 알림 — 2026-09-17

- CFA 비동기 작업을 사용하는 CFA/SEM의 시작, 진행 제목, 단계, 중단 버튼, 중단/완료/실패 알림을 현재 UI 언어에 연결했다.
- 작업 완료 시점의 현재 언어를 읽는다. 작업 상태와 진행률 계산, 종료 및 결과 갱신 로직은 변경하지 않았다.
- 실패 알림의 공통 문구만 번역하며 상세 엔진 오류는 원문으로 보존한다.
- `scripts/validate_structural_async_progress_i18n.R`: 8개 언어의 9개 문구, 모델명 치환, 5개 단계의 실제 알림 HTML, 반복 수 보존을 확인했다. 진행 상태 역행 방지 검사와 핸들러 구문 검사 통과.
- 공통 다국어 사전 검사와 diff 공백 검사 통과. 로그: `tmp/structural-async-progress-{validation,coverage}.log`.
- 이번 검증은 알림 렌더링 및 상태 처리 검사다. 실제 백그라운드 프로세스를 시작/중단하는 브라우저 통합 검증은 수행하지 않았다.
- 분석 결과 출력/저장 내용은 변경하지 않아 내보내기 검사를 재실행하지 않았다.

구조효과 및 PLS의 별도 비동기 알림, 엔진 상세 오류 등은 남은 점검 대상이다. 전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
