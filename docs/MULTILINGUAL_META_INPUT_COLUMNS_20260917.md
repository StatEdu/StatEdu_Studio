# 메타분석 입력 열과 조절변수 유형 — 2026-09-17

효과크기 입력 목록의 일반 열 제목 11개와 빈 목록 안내 열 제목, 조절변수 선택의 범주형/연속형 표시를 8개 언어로 연결했다(키 14개). 출판연도 선택 항목은 같은 번역을 재사용한다. Hedges' g, r, OR 효과척도 표기는 유지했다. 변경 대상은 입력 UI이며 분석 결과 본표·보조표·보고서가 아니다.

`validate_meta_input_status_i18n.R`에 열 제목, 빈 목록, 세 효과척도 및 실제 조절변수 선택 HTML 검사를 추가했다. 8개 언어와 한국어 복귀, 사용자 이름/자료/행 ID, 선택 항목의 내부 키와 선택 상태 유지 검사를 통과했다. 카탈로그 회귀 검사도 통과했다. 브라우저 실조작이나 내보내기 전체 검사를 수행한 것은 아니다.

입력 확인 상세, 조절변수 셀 내부 표현 및 분석 실패 오류는 추가 점검 대상이다. 설치본을 만들지 않았다.
