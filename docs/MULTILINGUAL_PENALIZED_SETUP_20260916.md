# 규제회귀 설정·입력 오류

2026-09-16. 설치본은 만들지 않았다.

- Elastic Net alpha 탐색, 선택 안정성 반복수, 중첩 CV 반복수, 선택 후 검정, 표본 분할 반복수와 두 도움말 등 설정 문구 7개를 번역했다.
- 종속변수 미선택/유형 오류, 독립변수 미선택, bootstrap 반복수 범위 오류 4개를 표시 시 UI 언어로 변환했다. 엔진 내부 오류 원문은 유지하고 실행 핸들러에서 변환한다. 미등록 외부 오류는 그대로 보존한다.
- `scripts/validate_penalized_setup_i18n.R`: Shiny testServer에서 실제 등록된 설정 UI를 8개 언어로 생성했다. 기대 번역의 존재, 영어 잔존 여부, input id/name/type/value/min/max/step/checked 동일성을 검사했다. 실제 함수 호출로 네 입력 오류를 발생시켰다. 브라우저에서의 시각 배치 검증은 아니다.
- `scripts/validate_penalized_menu.R`: 세 방법 실행, 단일 메뉴, 다중 종속변수 및 케이스/분할 변수 제외 검사 통과.
- i18n 계약 검사 통과.
- 설정 및 오류 표시만 변경해 분석 출력 파일은 재생성하지 않았다. 본표/보조표/계산 구조는 변경하지 않았다.
- 남은 범위: 선택 후 검정/전체 검정/여러 회 반복 CV의 실제 다국어 결과 및 진단 상태, 엔진 심층 오류, 브라우저 언어 왕복·도표. 전용 검증 근거 필요 화면 수는 19개로 유지된다.
