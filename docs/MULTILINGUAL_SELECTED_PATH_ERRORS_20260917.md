# 선택 구조경로 오류 안내 — 2026-09-17

- 비교 범위, 경로 미선택, 비교 가능한 잠재변수 경로 없음, 무효 경로, 중복 경로/ID, 동일화 제약에 따른 예상 밖 자유도 변화의 6종 안내를 추가 6개 언어로 번역했다.
- 기존 영어/한국어 경로와 엔진 판정을 유지한다. 무효 경로 오류의 경로 ID는 상세 구분자 뒤에서 추출해 원문 그대로 표시한다.
- `scripts/validate_selected_path_errors_i18n.R`: 12개 알려진 오류 형식을 8개 언어로 검사했다. 경로 ID `Review 사용자 <&> %s, edge.1`와 미등록 오류 원문 보존을 확인했다.
- 기존 PLS 오류·표본설계 오류 검사, 공통 다국어 사전 및 diff 공백 검사 통과.
- 로그: `tmp/selected-path-errors-{validation,coverage}.log` 및 기존 오류 검증 로그.
- 시험 메시지를 사용한 표시 함수 검사이며 실제 다집단 모형에서 각 오류를 발생시킨 통합 검증은 아니다. 결과표/내보내기를 변경하지 않아 저장 검사는 재실행하지 않았다.

일반적인 선택 경로 불일치 오류, 수정지수/특이행렬 등은 남은 점검 대상이다. 전체 다국어 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
