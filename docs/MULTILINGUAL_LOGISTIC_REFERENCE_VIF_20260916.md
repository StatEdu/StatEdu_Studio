# 로지스틱 기준 범주·VIF 안내

2026-09-16. 설치본은 만들지 않았다.

- 기준 범주 미지정 시 최솟값 사용 안내, 최대 VIF, 다중공선성 상태 3종, 주요 문제 없음, Wald 신뢰구간 설명, VIF 5/10 초과 안내 등 9개 문구를 번역했다.
- 원본 변수명·기준값·VIF 수치를 템플릿 인수로 보존한다. 계산 코드와 본표 생성 코드는 변경하지 않았다.
- 확장된 `scripts/validate_logistic_odds_i18n.R`: 8개 언어 통과. 실제 순서형·다항 출력의 기준 범주 및 정상 VIF 안내, 본표 영어/내용 동일성, 사용자 문자열 보호를 검사했다. VIF 주의·경고는 문구 fixture로 검사했다.
- 기존 로지스틱 UI 및 i18n 계약 검사 통과.
- 일본어 실제 결과와 문구 fixture의 현재·누적 HTML/Word/HWPX/Excel 내용 비교, PDF 생성 및 실제 PDF 텍스트 비교 통과.
- 표준 순서형·다항 일본어 출력 재조사에서 기존 장문 영어 설명 누락은 더 이상 탐지되지 않았다. 표의 `ord Model 1`/`cat Model 1` 표기는 남아 있다. 희소 셀·분리·희귀사건 등 다른 진단 분기는 후속 점검 대상이다. 전체 다국어 완료를 의미하지 않는다.
