# 요인점수 품질 보조표 다국어 점검 (2026-09-17)

## 변경 범위

- 요인점수 품질 보조표의 제목, 결정성/점수 신뢰도/해석 안내 열, 네 가지 판정과 설명 문단을 UI 언어로 표시한다.
- 일본어·중국어·스페인어·프랑스어·독일어·베트남어 번역 10개 항목을 보강했다. 기존 공통 번역도 재사용한다.
- 요인 이름과 사용자 라벨은 번역하지 않는다. 수치 산출 및 소수점 형식은 유지한다. 본표와 그림에는 변경이 없다.

## 검증

- `scripts/validate_factor_score_i18n.R`: 실제 ML 적합의 요인점수, 8개 언어, 네 가지 판정 분기, 사용자 문자열 `Review`, `Normality`, `Primary`, 한글 및 특수문자 보존을 확인했다.
- 실제 순서형 WLSMV 적합에서는 번들 lavaan이 점수 신뢰도를 반환하지 않아 보조표가 비어 있는 기존 동작을 확인했다. 순서형 설명 문구는 연속형 적합의 유효한 점수에 ordered 메타데이터를 적용한 표시용 fixture로 검증했다. 순서형 점수 계산 지원을 새로 구현한 것은 아니다.
- 공통 다국어 커버리지 검사 통과.
- 일본어 스냅샷으로 현재 결과 3개 표, 누적 결과 4개 표의 HTML/PDF/Word/HWPX/Excel 내보내기를 검증했다. PDF 추출 텍스트·표 내용·주석 및 HTML/Word 표 순서와 Excel 시트 수 검사 통과.
- 기록: `tmp/factor-score-validation.log`, `tmp/factor-score-coverage.log`, `tmp/factor-score-exports.log`, `tmp/factor-score-i18n/`.

설치본은 생성하지 않았다. 이 문서는 요인점수 보조표 범위의 검증이며 전체 분석 분기의 다국어 완료를 의미하지 않는다.
