# 적합도 참고 안내 다국어 검증 — 2026-09-17

## 변경

- 기준 기반 적합도 안내의 제목, 설명, 포화모형 안내, 모형별 상태 요약을 UI 언어로 표시한다.
- 추가 6개 언어의 참고 기준 셀을 번역하고 이미 번역한 셀을 중복 변환하지 않도록 보호한다.
- 자동 연구모형명은 번역하고 사용자 지정 비교 모형명은 보존한다. 한국어에서 사용자 모형명을 고정된 탐색적 수정모형 문구로 덮어쓰던 동작을 수정했다.
- 본 표와 적합도 계산은 변경하지 않았다.

## 검증 범위

- `scripts/validate_structural_fit_guidance_i18n.R`: 실제 ML CFA, 동일 적합 결과의 모형 비교, 실제 3지표 포화모형을 8개 언어로 확인했다(24조합).
- 적합도 수치가 원본 함수와 일치하고, 사용자 비교 이름 `Review 사용자 <&>`와 상태 요약·설명이 보존/번역되는지 검사했다.
- 공통 다국어 사전 검사 통과.
- 일본어 캡처의 현재·누적 HTML/PDF/Word/HWPX/Excel 내용 보존 검사 통과. PDF 실제 텍스트와 표지 검사 통과. 기존 추가 지표 표를 포함한 현재 23개/누적 31개 표 순서와 Excel 시트 수 일치.
- 캡처에는 아래의 기존 추가 적합도 표도 함께 포함하여 현재·누적 내보내기를 검증한다. 해당 추가 지표군 제목과 열의 전체 다국어 전환은 이번 변경 범위에 포함하지 않았으며 다음 점검 대상으로 남아 있다.
- 산출물: `tmp/structural-fit-guidance-i18n`; 로그: `tmp/structural-fit-guidance-{validation,coverage,exports}.log`.

전체 다국어 검증 완료를 의미하지 않는다. 설치본은 생성하지 않았다.
