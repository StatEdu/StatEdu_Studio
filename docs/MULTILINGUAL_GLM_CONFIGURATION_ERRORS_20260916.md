# GLM 설정 오류 다국어 점검

2026-09-16. 설치본은 만들지 않았다.

- 분석 가중치 개수 불일치, 잘못된 카운트 분포 고정 인수, 지원하지 않는 연결함수의 안내 3종을 번역했다.
- `scripts/validate_glm_configuration_errors_i18n.R`에서 실제 함수 호출로 세 오류를 재현하고 8개 언어의 기대 문구와 비교했다. 연결함수 오류의 분포 식별자는 원문을 보존한다.
- 같은 검사에서 `R/analysis_generalized.R`의 구문 트리를 읽어 직접 정의된 고유 오류 문구 30종을 추출했다. 정적 문자열 및 sprintf 템플릿 모두 영어 원문 보존과 7개 UI 언어의 번역 연결을 통과했다. 이는 모든 오류 분기를 실제 분석으로 재현했다는 의미가 아니다.
- 기존 MI/입력 오류 검사 및 i18n 계약 검사도 통과했다.
- 알림 표시와 번역 사전만 변경했다. 분석 계산, 본표, 보조표, 저장 경로는 변경하지 않았으며 출력 파일 재검증 대상은 없다. 실제 데스크톱 알림 시각 검증은 수행하지 않았다.
- 외부 라이브러리 상세 오류는 원문을 유지한다. 다른 분석의 UI/보조표/경고는 별도 점검 대상이며 전체 다국어 완료를 의미하지 않는다.
