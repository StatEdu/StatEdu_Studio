# Cox 개선의 전체 분석 효과 — 2026-09-14

유한값 행 검사 및 영향 잔차 재사용 개선을 전체 Cox 분석에서 함께 검증했다. 기준은 두 변경 전인 output/cox-finite-rows-20260914/baseline.R이며, 현재 소스에는 두 변경이 모두 적용되어 있다. 이번에는 실행 코드를 추가로 수정하지 않았다.

## 전체 분석 시간

번들 R 4.5.3, 앱 설정과 기존 모듈 캐시 사용, 자동 설치 비활성화. 새 프로세스에서 변경 전후 3쌍을 순차 실행하고 두 번째 쌍은 순서를 반대로 했다. 스냅샷의 함수들을 같은 전역 환경에 연결했다. 각 프로세스에서 1천·1만·5만 행 순서로 실행했다.

자료는 숫자 공변량 10개, 첫 공변량에 따른 위험률을 가진 합성 생존시간, 약 70% 사건률이다. 모형 적합·진단·결과표 구성을 포함하는 prepare_cox_analysis_result 전체 호출을 측정했다. HTML 생성과 파일 저장은 측정 밖이다.

| 행 수 | 변경 전 3회(초) | 변경 후 3회(초) | 중앙값 전→후 |
|---|---|---|---|
| 1,000 | 0.77 / 0.78 / 0.78 | 0.80 / 0.80 / 0.79 | 0.78→0.80 |
| 10,000 | 0.12 / 0.13 / 0.12 | 0.11 / 0.11 / 0.13 | 0.12→0.11 |
| 50,000 | 1.31 / 1.29 / 1.31 | 1.19 / 1.21 / 1.18 | 1.31→1.19 |

5만 행에서 전체 분석 시간이 약 9%, 절대 0.12초 감소했다. 1만 행 차이는 작고 범위가 겹친다. 첫 1천 행 조건은 중앙값이 0.02초 증가했으며, 첫 호출의 초기화·컴파일 등을 포함한다. 첫 조건과 후속 조건은 준비 상태가 다르므로 행 수 사이의 절대 시간을 직접 비교하지 않는다.

이 결과는 진단 함수 단독에서 관찰한 43%·51% 단축을 전체 분석 시간에 그대로 적용할 수 없음을 보여준다. 자료 크기와 분포 및 측정 범위가 다르므로 단독 측정 절감량을 합산하지 않는다. 앱 시작·화면 표시·내보내기를 포함하는 사용자 체감 전체 시간은 이번 측정 대상이 아니다.

## 정확성 및 실제 경로 확인

- 9개 변경 전후 결과 쌍의 전체 수치·표·진단·난수 상태가 identical(..., num.eq=FALSE)로 일치했다.
- Cox의 수식/terms에 포함된 네 `.Environment` 참조만 측정 종료 후 동일한 전역 환경으로 정규화해 저장했다. 수치 반올림이나 허용오차는 사용하지 않았다. 원본 환경 참조까지 바이트 단위로 같다는 주장은 아니다.
- 세 크기의 HTML 및 의존성 객체도 일치했다.
- 실제 분석 행 수는 1천/1만/5만, 추정 계수는 모두 10개였다. 각 결과의 영향 진단 10행에서 최대 절대 DFBETAS가 모두 유한했으며 다중공선성 진단도 모두 10행이었다. 실패한 진단을 건너뛰어 빨라진 결과가 아님을 확인했다.
- 이 경로 확인에 사용한 현재 각 첫 측정 결과의 사건 수는 697/6,975/34,977, 경고·메시지는 모두 0건이었다.
- 실행 모듈과 current.R 스냅샷의 SHA256이 일치한다: D5745C069842330F0F63BC748B8719942B9E837D9FA856F34C24D7D32A898049.

산출물: output/cox-combined-20260914/의 baseline.R, current.R, measure.R, compare.R, coverage.R, summary.csv, coverage.csv 및 측정 결과 객체. 전체 서버와 PDF/Word/HWPX/Excel 변환은 이번에 재검증하지 않았다. 설치 파일은 재빌드하지 않았다.
