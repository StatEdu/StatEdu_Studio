const assert=require('node:assert/strict');const {chromium}=require(process.env.STATEDU_PLAYWRIGHT_MODULE);
(async()=>{const b=await chromium.launch({channel:'chrome',headless:true});try{const p=await b.newPage({viewport:{width:1550,height:1100}});await p.goto('http://127.0.0.1:3869');const pre='penalized_regularized';for(const [v,r] of [['x','x'],['z','x'],['y','y']]){await p.locator(`[data-input-id="${pre}_available"] [data-value="${v}"]`).click();await p.locator(`#${pre}_move_${r}`).click();await p.locator(`[data-input-id="${pre}_${r}"] [data-value="${v}"]`).waitFor();}
async function drag(first,last){const a=await p.locator(`[data-input-id="${pre}_x"] [data-value="${first}"]`).boundingBox(),z=await p.locator(`[data-input-id="${pre}_x"] [data-value="${last}"]`).boundingBox();await p.mouse.move(a.x+80,a.y+a.height/2);await p.mouse.down();await p.mouse.move(z.x+80,z.y+z.height-2,{steps:15});await p.mouse.up();await p.waitForFunction(first=>document.querySelector('[data-input-id="penalized_regularized_x"] .analysis-transfer-option:last-child').dataset.value===first,first);}
await drag('x','z');await drag('z','x');await p.locator('#penalized_regularized_x_up').click();console.log('PASS: mouse reorder persists through server renders, both directions');}finally{await b.close();}})().catch(e=>{console.error(e);process.exit(1)});
